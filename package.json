{
  "name": "stockvision-portal-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package script delegator for StockVision prediction portal",
  "scripts": {
    "postinstall": "npm install --prefix frontend-react",
    "dev": "npm run dev --prefix frontend-react",
    "build": "npm run build --prefix frontend-react",
    "lint": "npm run lint --prefix frontend-react"
  }
}